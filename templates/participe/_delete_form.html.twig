<form method="post" action="{{ path('app_participe_delete', {'id': participe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participe.id) }}">
    <button class="btn">Delete</button>
</form>
